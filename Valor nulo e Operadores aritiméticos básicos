//Valor nulo
fun main() {
   var month:Int? = null
   //Atribui valor null corretamente a month
   println(month)
   
   //var day:Int = null
   //Neste caso irá apresentar erro de compilação. Null can not be a value of type Int
   
   //Operador aritimético
   /*val count = 10
   val times = 9
   val r = times + count
   println(r)
   println(count.plus(times))*/
   
  //Exluindo a varável r, e somando count com times, onde será relizado a soma de times com count e times novamente.
   /*val count = 10
   var times = 9
   times += count
   println(times)
   println(count.plus(times))
   
   //Concatenando times com count*/
   var count = "Ola, "
   var times = "mundo!"
   count += times
       println(count) 
}